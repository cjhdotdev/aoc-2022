<?php

namespace Tests\Unit;

// $collect = Str::of($str)
//      ->split('//')
//      ->filter()
//      ->chunk(4)
//      ->transform(fn($codes) => $codes->splice(1, 1))
//      ->flatten();

it('can parse the code block', function () {
});
